;* * *  Small-C/Plus Version 1.06a for the TMS 9900 - Cameron * * *
;       Cain, Van Zandt, Hendrix, Yorston, Cameron
;          Original Z80  Version 21st April 1990       
;
R0	EQU 0
R1	EQU 1
R2	EQU 2
R3	EQU 3
R4	EQU 4
R8	EQU 8
SL	EQU 9
R9	EQU 9
SP	EQU 10
WP	EQU 13
R11	EQU 11
	DXOP CALL,6
	DXOP RET,7
	DXOP WHEX,10
	DXOP WRITE,12    ;WRITE CHAR IN MSB 
	DXOP	MESG,14
	DXOP DEBUG,15    ;TRACE THE PRECEDING INSTRUCTION, PC,ST and REGISTERS 

;-------- START MODULE -----------
	STWP WP 
	B @_main## ; This calls iolib99 entry point
;/*
;** SMALLC99.C -- M38d native TMS99000/SBC compiler driver
;**
;** Active TMS99105 SBC driver, built as SMALLC99.EXE. The older 8086
;** driver remains in SMALLC99.C for reference only. This driver reuses
;** the common Hendrix front end and overlay manager with the CC_CD99
;** resident output layer and CC_CG99 template overlay.
;*/
;#define YES 1
;#define NO  0
;#define NAMEMAX 14
;extern PORT_INIT();
;extern R_GETOPTS();
;extern R_PREP();
;extern ccinit();
;extern setcodes();
;extern parse();
;extern header();
;extern trailer();
;extern fopen();
;extern fclose();
;extern puts();
;extern int srcunit;
;extern int outunit;
;extern int eof;
;extern int tmsfail;
;char srcname[NAMEMAX+1];
srcname:	BSS 15
	EVEN
;char outname[NAMEMAX+1];
outname:	BSS 15
	EVEN
;int verbose;
verbose:	BSS 2
;main(argc, argv)
	EVEN
main:
;int argc;
;char **argv;
;{
;modstk(newsp,save)
;    srcunit = 0;
;modstk(newsp,save)
;const1(val)
	CLR R4
;putmem(sym)
	MOV R4,@srcunit
;    outunit = 0;
;const1(val)
	CLR R4
;putmem(sym)
	MOV R4,@outunit
;    verbose = NO;
;const1(val)
	CLR R4
;putmem(sym)
	MOV R4,@verbose
;    PORT_INIT();                 /* initialise target environment */
;zcall()
	CALL @PORT_INIT
;modstk(newsp,save)
;    if(ask(argc, argv) == NO)   /* get user options */
;getloc(sym, off)
	MOV @4(SP),R4 ;optimised load
;zpush()
	DECT SP
	MOV R4,*SP
;getloc(sym, off)
	MOV @4(SP),R4 ;optimised load
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @ask
;modstk(newsp,save)
	AI SP,4
;eq0(label)
	MOV R4,R4
	JEQ $+6
	B @cc2
;        return;
;modstk(newsp,save)
	RET
;    compinit();                 /* initialise compiler and TMS backend */
cc2:
;zcall()
	CALL @compinit
;modstk(newsp,save)
;    if(openfile() == NO)        /* open initial input and output */
;zcall()
	CALL @openfile
;modstk(newsp,save)
;eq0(label)
	MOV R4,R4
	JEQ $+6
	B @cc3
;        return;
;modstk(newsp,save)
	RET
;    firstline();                /* fetch first source line */
cc3:
;zcall()
	CALL @firstline
;modstk(newsp,save)
;    header();                   /* emit native TMS module preamble */
;zcall()
	CALL @header
;modstk(newsp,save)
;    parse();                    /* process ALL input */
;zcall()
	CALL @parse
;modstk(newsp,save)
;    trailer();                  /* close native TMS module */
;zcall()
	CALL @trailer
;modstk(newsp,save)
;    if(closefile() == NO)       /* explicitly close both files */
;zcall()
	CALL @closefile
;modstk(newsp,save)
;eq0(label)
	MOV R4,R4
	JEQ $+6
	B @cc4
;        return;
;modstk(newsp,save)
	RET
;    finish();                   /* report compiler completion */
cc4:
;zcall()
	CALL @finish
;modstk(newsp,save)
;}
;modstk(newsp,save)
;modstk(newsp,save)
	RET
;ask(argc, argv)
	EVEN
ask:
;int argc;
;char **argv;
;{
;    if(R_GETOPTS(argc, argv) == NO)
;modstk(newsp,save)
;getloc(sym, off)
	MOV @4(SP),R4 ;optimised load
;zpush()
	DECT SP
	MOV R4,*SP
;getloc(sym, off)
	MOV @4(SP),R4 ;optimised load
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @R_GETOPTS
;modstk(newsp,save)
	AI SP,4
;eq0(label)
	MOV R4,R4
	JEQ $+6
	B @cc6
;        return NO;
;const1(val)
	CLR R4
;modstk(newsp,save)
	RET
;    if(verbose) {
cc6:
;getmem(sym)
	MOV @verbose,R4
	MOV R4,R4
	JNE $+6
	B @cc7
;        puts("SMALLC99: compiling ");
;modstk(newsp,save)
;constant(lval)
	LI R4,cc5+0
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;        puts(srcname);
;address(ptr)
	LI R4,srcname
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;        puts(" -> ");
;constant(lval)
	LI R4,cc5+21
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;        puts(outname);
;address(ptr)
	LI R4,outname
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;        puts("\n");
;constant(lval)
	LI R4,cc5+26
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;    }
;modstk(newsp,save)
;    return YES;
cc7:
;const1(val)
	LI R4,1
;modstk(newsp,save)
	RET
;}
cc5:	BYTE 83,77,65,76,76,67,57,57,58,32,99,111
	BYTE 109,112,105,108,105,110,103,32,0,32,45,62
	BYTE 32,0,13,0
;compinit()
	EVEN
compinit:
;{
;    ccinit();
;modstk(newsp,save)
;zcall()
	CALL @ccinit
;modstk(newsp,save)
;    setcodes();
;zcall()
	CALL @setcodes
;modstk(newsp,save)
;}
;modstk(newsp,save)
;modstk(newsp,save)
	RET
;openfile()
	EVEN
openfile:
;{
;    srcunit = fopen(srcname, "r");
;modstk(newsp,save)
;address(ptr)
	LI R4,srcname
;zpush()
	DECT SP
	MOV R4,*SP
;constant(lval)
	LI R4,cc9+0
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @fopen
;modstk(newsp,save)
	AI SP,4
;putmem(sym)
	MOV R4,@srcunit
;    if(srcunit == 0) {
;getmem(sym)
	MOV @srcunit,R4
;eq0(label)
	MOV R4,R4
	JEQ $+6
	B @cc10
;        puts("SMALLC99: cannot open ");
;modstk(newsp,save)
;constant(lval)
	LI R4,cc9+2
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;        puts(srcname);
;address(ptr)
	LI R4,srcname
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;        puts("\n");
;constant(lval)
	LI R4,cc9+25
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;        return NO;
;const1(val)
	CLR R4
;modstk(newsp,save)
	RET
;    }
;    outunit = fopen(outname, "w");
cc10:
;address(ptr)
	LI R4,outname
;zpush()
	DECT SP
	MOV R4,*SP
;constant(lval)
	LI R4,cc9+27
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @fopen
;modstk(newsp,save)
	AI SP,4
;putmem(sym)
	MOV R4,@outunit
;    if(outunit == 0) {
;getmem(sym)
	MOV @outunit,R4
;eq0(label)
	MOV R4,R4
	JEQ $+6
	B @cc11
;        fclose(srcunit);
;modstk(newsp,save)
;getmem(sym)
	MOV @srcunit,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @fclose
;modstk(newsp,save)
	INCT SP
;        srcunit = 0;
;const1(val)
	CLR R4
;putmem(sym)
	MOV R4,@srcunit
;        puts("SMALLC99: cannot create ");
;constant(lval)
	LI R4,cc9+29
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;        puts(outname);
;address(ptr)
	LI R4,outname
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;        puts("\n");
;constant(lval)
	LI R4,cc9+54
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;        return NO;
;const1(val)
	CLR R4
;modstk(newsp,save)
	RET
;    }
;    return YES;
cc11:
;const1(val)
	LI R4,1
;modstk(newsp,save)
	RET
;}
cc9:	BYTE 114,0,83,77,65,76,76,67,57,57,58,32
	BYTE 99,97,110,110,111,116,32,111,112,101,110,32
	BYTE 0,13,0,119,0,83,77,65,76,76,67,57
	BYTE 57,58,32,99,97,110,110,111,116,32,99,114
	BYTE 101,97,116,101,32,0,13,0
;firstline()
	EVEN
firstline:
;{
;    eof = 0;
;modstk(newsp,save)
;const1(val)
	CLR R4
;putmem(sym)
	MOV R4,@eof
;    R_PREP();
;zcall()
	CALL @R_PREP
;modstk(newsp,save)
;}
;modstk(newsp,save)
;modstk(newsp,save)
	RET
;closefile()
	EVEN
closefile:
;{
;    if(fclose(outunit) == 0) {
;modstk(newsp,save)
;getmem(sym)
	MOV @outunit,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @fclose
;modstk(newsp,save)
	INCT SP
;eq0(label)
	MOV R4,R4
	JEQ $+6
	B @cc14
;        outunit = 0;
;modstk(newsp,save)
;const1(val)
	CLR R4
;putmem(sym)
	MOV R4,@outunit
;        fclose(srcunit);
;getmem(sym)
	MOV @srcunit,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @fclose
;modstk(newsp,save)
	INCT SP
;        srcunit = 0;
;const1(val)
	CLR R4
;putmem(sym)
	MOV R4,@srcunit
;        puts("SMALLC99: cannot close ");
;constant(lval)
	LI R4,cc13+0
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;        puts(outname);
;address(ptr)
	LI R4,outname
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;        puts("\n");
;constant(lval)
	LI R4,cc13+24
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;        return NO;
;const1(val)
	CLR R4
;modstk(newsp,save)
	RET
;    }
;    outunit = 0;
cc14:
;const1(val)
	CLR R4
;putmem(sym)
	MOV R4,@outunit
;    fclose(srcunit);
;getmem(sym)
	MOV @srcunit,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @fclose
;modstk(newsp,save)
	INCT SP
;    srcunit = 0;
;const1(val)
	CLR R4
;putmem(sym)
	MOV R4,@srcunit
;    return YES;
;const1(val)
	LI R4,1
;modstk(newsp,save)
	RET
;}
cc13:	BYTE 83,77,65,76,76,67,57,57,58,32,99,97
	BYTE 110,110,111,116,32,99,108,111,115,101,32,0
	BYTE 13,0
;finish()
	EVEN
finish:
;{
;    if(tmsfail) {
;modstk(newsp,save)
;getmem(sym)
	MOV @tmsfail,R4
	MOV R4,R4
	JNE $+6
	B @cc16
;        puts("SMALLC99: compilation failed; output is not valid assembly\n");
;modstk(newsp,save)
;constant(lval)
	LI R4,cc15+0
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;        return;
;modstk(newsp,save)
	RET
;    }
;    if(eof == 0)
cc16:
;getmem(sym)
	MOV @eof,R4
;eq0(label)
	MOV R4,R4
	JEQ $+6
	B @cc17
;        puts("SMALLC99: warning: parse returned before EOF\n");
;constant(lval)
	LI R4,cc15+60
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;    puts("SMALLC99: wrote ");
cc17:
;constant(lval)
	LI R4,cc15+106
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;    puts(outname);
;address(ptr)
	LI R4,outname
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;    puts("\n");
;constant(lval)
	LI R4,cc15+123
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;}
;modstk(newsp,save)
;modstk(newsp,save)
	RET
cc15:	BYTE 83,77,65,76,76,67,57,57,58,32,99,111
	BYTE 109,112,105,108,97,116,105,111,110,32,102,97
	BYTE 105,108,101,100,59,32,111,117,116,112,117,116
	BYTE 32,105,115,32,110,111,116,32,118,97,108,105
	BYTE 100,32,97,115,115,101,109,98,108,121,13,0
	BYTE 83,77,65,76,76,67,57,57,58,32,119,97
	BYTE 114,110,105,110,103,58,32,112,97,114,115,101
	BYTE 32,114,101,116,117,114,110,101,100,32,98,101
	BYTE 102,111,114,101,32,69,79,70,13,0,83,77
	BYTE 65,76,76,67,57,57,58,32,119,114,111,116
	BYTE 101,32,0,13,0
	EVEN
	ENT compinit
	ENT main
	EXT outunit
	EXT puts
	EXT fopen
	EXT R_PREP
	ENT ask
	EXT PORT_INIT
	EXT ccinit
	EXT eof
	ENT firstline
	ENT openfile
	EXT parse
	EXT header
	EXT fclose
	ENT verbose
	ENT srcname
	ENT closefile
	EXT trailer
	ENT finish
	EXT setcodes
	EXT tmsfail
	EXT R_GETOPTS
	ENT outname
	EXT srcunit
	EVEN
	END

; --- End of Compilation ---

;* * *  Small-C/Plus Version 1.06a for the TMS 9900 - Cameron * * *
;       Cain, Van Zandt, Hendrix, Yorston, Cameron
;          Original Z80  Version 21st April 1990       
;
R0	EQU 0
R1	EQU 1
R2	EQU 2
R3	EQU 3
R4	EQU 4
R5	EQU 5
R6	EQU 6
R7	EQU 7
R8	EQU 8
SL	EQU 9
R9	EQU 9
SP	EQU 10
WP	EQU 13
R11	EQU 11
	DXOP CALL,6
	DXOP RET,7
	DXOP WHEX,10
	DXOP WRITE,12    ;WRITE CHAR IN MSB 
	DXOP	MESG,14
	DXOP DEBUG,15    ;TRACE THE PRECEDING INSTRUCTION, PC,ST and REGISTERS 

;-------- START MODULE -----------
	NAM OVL_RPT
;/* OVL_RPT.C -- FILESTAT formatted-report overlay. */
;#asm
        AORG    08000H
;extern puts();
;extern putnum();
;extern putstat();
;extern int nline;
;extern int nword;
;extern int nchar;
;extern int nalpha;
;extern int ndigit;
;extern int nspace;
;extern int npunct;
;extern int longlen;
;extern char longword[];
;extern char *fsname;
;report()
	EVEN
report:
;{
;modstk(newsp,save)
;    puts("\nFILESTAT report\n");
;modstk(newsp,save)
;constant(lval)
	LI R4,cc1+0
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;    puts("File: ");
;constant(lval)
	LI R4,cc1+18
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;    puts(fsname);
;getmem(sym)
	MOV @fsname,R4
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;    puts("\n");
;constant(lval)
	LI R4,cc1+25
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;    putstat("Lines", nline);
;constant(lval)
	LI R4,cc1+27
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @nline,R4
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putstat
;modstk(newsp,save)
	AI SP,4
;    putstat("Words", nword);
;constant(lval)
	LI R4,cc1+33
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @nword,R4
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putstat
;modstk(newsp,save)
	AI SP,4
;    putstat("Characters", nchar);
;constant(lval)
	LI R4,cc1+39
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @nchar,R4
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putstat
;modstk(newsp,save)
	AI SP,4
;    putstat("Letters", nalpha);
;constant(lval)
	LI R4,cc1+50
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @nalpha,R4
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putstat
;modstk(newsp,save)
	AI SP,4
;    putstat("Digits", ndigit);
;constant(lval)
	LI R4,cc1+58
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @ndigit,R4
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putstat
;modstk(newsp,save)
	AI SP,4
;    putstat("Whitespace", nspace);
;constant(lval)
	LI R4,cc1+65
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @nspace,R4
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putstat
;modstk(newsp,save)
	AI SP,4
;    putstat("Punctuation", npunct);
;constant(lval)
	LI R4,cc1+76
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @npunct,R4
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putstat
;modstk(newsp,save)
	AI SP,4
;    puts("Longest word: ");
;constant(lval)
	LI R4,cc1+88
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;    if(longword[0])
;address(ptr)
	LI R4,longword
;indirect ccgchar
	MOVB *R4,R4
	SRA R4,8
	MOV R4,R4
	JNE $+6
	B @cc2
;        puts(longword);
;address(ptr)
	LI R4,longword
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;    else
;jump(label)
	B @cc3
cc2:
;        puts("(none)");
;constant(lval)
	LI R4,cc1+103
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
cc3:
;    puts(" (");
;constant(lval)
	LI R4,cc1+110
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;    putnum(longlen);
;getmem(sym)
	MOV @longlen,R4
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putnum
;modstk(newsp,save)
	INCT SP
;    puts(")\n");
;constant(lval)
	LI R4,cc1+113
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;}
;modstk(newsp,save)
;modstk(newsp,save)
	RET
cc1:	BYTE 13,70,73,76,69,83,84,65,84,32,114,101
	BYTE 112,111,114,116,13,0,70,105,108,101,58,32
	BYTE 0,13,0,76,105,110,101,115,0,87,111,114
	BYTE 100,115,0,67,104,97,114,97,99,116,101,114
	BYTE 115,0,76,101,116,116,101,114,115,0,68,105
	BYTE 103,105,116,115,0,87,104,105,116,101,115,112
	BYTE 97,99,101,0,80,117,110,99,116,117,97,116
	BYTE 105,111,110,0,76,111,110,103,101,115,116,32
	BYTE 119,111,114,100,58,32,0,40,110,111,110,101
	BYTE 41,0,32,40,0,41,13,0
	EXT nalpha
	EXT fsname
	EXT ndigit
	EXT putstat
	EXT longword
	EXT puts
	EXT nchar
	EXT nspace
	ENT report
	EXT nline
	EXT npunct
	EXT nword
	EXT longlen
	EXT putnum
	EVEN
	END

; --- End of Compilation ---

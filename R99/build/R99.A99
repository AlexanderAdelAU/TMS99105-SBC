;* * *  Small-C/Plus Version 1.06a for the TMS 9900 - Cameron * * *
;       Cain, Van Zandt, Hendrix, Yorston, Cameron
;          Original Z80  Version 21st April 1990       
;
R0	EQU 0
R1	EQU 1
R2	EQU 2
R3	EQU 3
R4	EQU 4
R8	EQU 8
SL	EQU 9
R9	EQU 9
SP	EQU 10
WP	EQU 13
R11	EQU 11
	DXOP CALL,6
	DXOP RET,7
	DXOP WHEX,10
	DXOP WRITE,12    ;WRITE CHAR IN MSB 
	DXOP	MESG,14
	DXOP DEBUG,15    ;TRACE THE PRECEDING INSTRUCTION, PC,ST and REGISTERS 

;-------- START MODULE -----------
	STWP WP 
	B @_main## ; This calls iolib99 entry point
;/*
; 9900 Cross-Assembler  v. 2.1
; May, 1980
; Copyright (c) 1980 William C. Colley, III.
; File:	r99.c
; It all begins here.
; */
;/*  Get globals:  */
;#include "stdio.h"
;/*
;** STDIO.H -- Standard Small-C Definitions
;**
;** Copyright 1984  L. E. Payne and J. E. Hendrix
;*/
;#define stdin    0
;#define stdout   1
;#define stderr   2
;#define	ERR		(-2)
;#define EOF		(-1)
;#define YES      1
;#define NO       0
;#define NULL     0
;#define CR      0x0D /* '\r' */
;#define LF      0x0A /* '\n' */
;#define BELL     7
;#define SPACE  ' '
;#define NEWLINE LF      /*23*/ /*45*/
;#define FILE char
;/* Flags for the iobuf structure  */
;#define	_IOREAD	1 /* currently reading */
;#define	_IOWRT	2 /* currently writing */
;#define	_IORW	0x0080 /* opened as "r+w" */
;/* Flags for lseek and fseek */
;#define SEEK_SET  0
;#define SEEK_CUR  1
;#define SEEK_END  2
;#include "R99CFG.h"
;/*
; * R99 build-size configuration.
; *
; * TEMPORARY SBC TESTING PROFILE.
; *
; * Goal: keep the flat R99.COM image below >C000 when loaded at >0500.
; * These reduced capacities are for initial board bring-up only.
; */
;#define LINLEN          128	/*  Must be at least 102: the 4-symbols-per-line table
;                                dump at the end of main writes 4 * 25 + 2 bytes into
;                                linbuf.  At 96 it spilled 6 bytes into binbuf.  */
;#define BUFSIZE          16
;#define SYMBOLS         101	/*  MUST BE ODD - slookup's probe step is d = 1, 3, 5, ...
;                                and its "table full" test is d == SYMBOLS, which an even
;                                SYMBOLS never reaches.  With a power of two the probe
;                                only ever visits 4 of the slots (h advances by k*k).  */
;#define INCLUDE_DEPTH     1
;#define MAXXREF            4
;#include "R99gbl.h"
;/*
; TMS990 Series Cross-Assembler  v. 1.0
; January, 1985
; Copyright (c) 1980 William C. Colley, III.
; Modified for the 990 Series by Alexander Cameron.
; File:	a99.gbl
; Global macro substitutions and external variable declarations.
; */
;/*  Build-size profile.  */
;/*  Define symbol table parameters:  REL files now allow 4 bits to define
; * the SYMLEN. The original spec allowed 3 bits
; *
; */
;#define	SYMLEN	16	/*  Symbol buffer size: 15 chars + terminating null. */
;#define	PADDING	"               "	/*  SYMLEN - 1 blanks.	15		*/
;#define ENTBIT 4	/* Sets ENTRY bit in symflg 			*/
;#define EQUBIT 16	/* Equivalence - defined via an equate 		*/
;#define DUPBIT 32	/* Equivalence - defined via an equate 		*/
;#define EXTBIT 8	/* Sets EXTERNAL bit in symflg			*/
;#define	DEFBIT	2	/* Sets DEFINED bit				*/
;#define RELBIT 1	/* Sets RELOCATION bit in symflg		*/
;#define COMBIT 12	/* Sets COMMON bits in symflg			*/
;/*  Number of if statements that can be nested:  */
;#define	IFDEPTH	16
;/*  BDOS functions called directly:  */
;#define	LISTOUT	5	/*  Put character to list device.		*/
;#define	LOGDISK	14	/*  Log in disk drive.				*/
;#define	GETDISK	25	/*  Get currently logged in disk.		*/
;/*  Define flag values:  */
;#define	TRUE	1	/*  General use.				*/
;#define	FALSE	0
;#define	SKIP	TRUE	/*  Used with skip flag in getchr.		*/
;#define	NOSKIP	FALSE
;#define	BIGST	TRUE	/*  Used to tell kind of string in getitem.	*/
;#define	SMALST	FALSE
;#define	NOCODE	0	/*  Used to tell the hex generator what to do.	*/
;#define	PUTCODE	1
;#define	FLUSH	2
;#define	NOMORE	3
;#define EXTADDR 4
;/*  File descriptors:  */
;#define	LODISK	0	/*  Lowest numbered disk.			*/
;#define	NOFILE	-1	/*  No file specified.				*/
;#define	CONO	-2	/*  Console output.				*/
;#define	LST	   -3	/*  List device.				*/
;/*  Items can have the following attributes:  */
;#define	ALPHA	0	/*  Alphabetic character.			*/
;#define	NUMERIC	1	/*  Numeric digit (0-9).			*/
;#define	END_LIN	2	/*  End-of-Line marker (cr or ;).		*/
;#define	COMMA	3	/*  Field separator (,).			*/
;#define	OPERATR	4	/*  Operator (* - GE < ( SHR etc.).		*/
;#define	BAS_DES	5	/*  Base designator ($ % @).			*/
;#define	QUOTE	6	/*  String delimiter (" ').			*/
;#define	TRASH	7
;#define	VALUE	8	/*  Evaluated expression.			*/
;#define	BLANK	10	/*  White space (spc tab).			*/
;#define COLON	12
;#define HATCH 	13
;/*  Some tokens for composite operators:  */
;#define GTTKN		'>'
;#define EQTKN		'='
;#define LTTKN		'<'
;#define	NO_OPR	0	/*  No operator.				*/
;#define	GRTEQ	1	/*  Greater than or equal to.			*/
;#define	NOTEQ	2	/*  Not equal to.				*/
;#define	LESEQ	3	/*  Less than or equal to.			*/
;#define	AND	4	/*  And.					*/
;#define	OR	5	/*  Or.						*/
;#define	XOR	6	/*  Exclusive or.				*/
;#define	NOT	7	/*  1's complement.				*/
;#define	MOD	8	/*  Mod--divide and return remainder.		*/
;#define	SHL	9	/*  Shift left.					*/
;#define	SHR	10	/*  Shift right.				*/
;#define	HIGH	11	/*  High byte of.				*/
;#define	LOW	12	/*  Low byte of.				*/
;/*  Operator precedence values:  */
;#define	UOP1	0	/*  Unary +, unary -.				*/
;#define	MULT	1	/*  *, /, MOD, SHL, SHR.			*/
;#define	ADDIT	2	/*  Binary +, binary -.				*/
;#define	RELAT	3	/*  >, =, <, >=, <>, <=.			*/
;#define	UOP2	4	/*  NOT.					*/
;#define	LOG1	5	/*  And.					*/
;#define	LOG2	6	/*  OR, XOR.					*/
;#define	UOP3	7	/*  HIGH, LOW.					*/
;#define	RPREN	8	/*  ).						*/
;#define	LPREN	9	/*  (.						*/
;#define	ENDEX	10	/*  CR, ;, ,.					*/
;#define	START	11	/*  Start of expression.			*/
;/*  Bits of opcode attribute byte.  */
;#define	PSOP	0x80	/*  Pseudo-op.					*/
;#define	DIROK	0x40	/*  Non-pseudo-op is OK for direct addressing.	*/
;#define	IFGROUP	0x40	/*  Pseudo-op is IF, ELSE, ENDI.		*/
;/*  Address modes for the 9900 instruction set 				*/
;#define WREG		0
;#define	INDIRECT 	1
;#define SYMBOLIC	2
;#define INDEXED 	3
;#define AUTOINC		4
;/*  Misc control chars */
;#define	LFCHAR	10	/*  Line feed.  NOTE: smallcp compiles '\n' as CR
;			    (13), so the LF has to be spelled out.  Never use
;			    '\r' either - smallcp reads it as the letter 'r'.  */
;#define CPMEOF	0x1A
;#define CTLS	19	/* Control S for stopping */
;#define CTLC    0x03	/* Control C aborts */
;#define NOSORT TRUE	/* Controls whether or not we want the table sorted */
;#define SORT FALSE
;#define INTEL8 TRUE    /* Generate Intel 16 or 32 Format */
;#define FILE char
;struct diskbuf {
;	int fd;
;	unsigned char *pointr;
;	unsigned char *endpoint;
;	unsigned char space[BUFSIZE];
;	FILE *fp;
;};
;extern struct diskbuf sorbuf, lstbuf, hexbuf;
;/*  Set up the symbol table: */
;struct symbtbl {
;	char symname[SYMLEN];
;	unsigned symvalu;
;	unsigned char symflg;
;	unsigned char sympad;	/*  keeps the entry EVEN in length - see below  */
;};
;/*  sympad is not optional.  Without it an entry is 19 bytes, so every odd
;    numbered slot starts on an ODD address and symvalu (offset 16) lands on
;    an odd address too.  The 99105 forces word accesses even, so the store
;    goes to offset 15/16 instead of 16/17 and wipes symname[15].  The value
;    reads back consistently, but the name no longer matches, so symcmp fails
;    and the same symbol gets added twice.  */
;extern struct symbtbl symtbl[SYMBOLS], *symend, *sympoint;
;/*  If stack and stack pointer.  */
;extern unsigned ifsp;
;extern unsigned ifstack[IFDEPTH + 1];
;/*  Buffer to hold current line:  */
;extern char *linptr, linbuf[LINLEN];
;/*  Buffer to hold the code generated by a line.  */
;extern unsigned char binbuf[LINLEN], *bptr;
;extern int nbytes;
;/*  Buffers for the hex generator.  */
;extern char chksum, hxbytes, *hxlnptr, hxlnbuf[88];
;/*  Miscellanious mailboxes:  */
;extern unsigned char extflg; /*  Indicates external symbol			*/
;extern unsigned char entflg; /*  Indicates and entry symbol			*/
;extern unsigned char relflg; /*  Indicates whether inst operand need relocation */
;extern int errcode; /*  Error records.				*/
;extern int errcount;
;extern int evalerr; /*  Flag to tell of error in eval.		*/
;extern unsigned backflg, oldattr; /*  Item push-back buffer.			*/
;extern unsigned oldvalu;
;extern int curdrive; /*  Place to save drive that was current
; disk when assembly started.		*/
;extern int hexflg; /*  Flag for asmline to tell hex
; generator what to do.			*/
;extern int directok; /*  All symbols on line pre-defined.		*/
;extern unsigned pc; /*  Assembly program counter.			*/
;extern unsigned progsize;
;extern unsigned address; /*  Address to be put into listed line.		*/
;extern char pass; /*  Which pass the assembly is in.		*/
;extern char addrmode;
;extern char quitflag; /*  Used in eval to detect INDEXED expressions  */
;/*added by [AC]  */
;extern unsigned char quoteflg;
;/*-----------
; rel variables
; ------------*/
;extern unsigned char outchunk, /* current output chunk in REL file */
;outrem, /* remaining bits in outchunk */
;item, /* current item code */
;type; /* type field */
;extern unsigned field; /* current bit field */
;extern char symbol[SYMLEN], /* current string */
;progname[SYMLEN]; /* programme module's name */
;extern unsigned nssymbols;
;extern unsigned itemflg[3]; /* type of item found in getitem
; itemflg[0] = current item found
; itemflg[1] = operand 1 item type
; itemflg[2] = operand 2 item type
; */
;extern unsigned symptr[3]; /* external or entry symbol pointer
; symptr[0] -> current symbol
; symptr[1] -> operand 1 symbol
; symptr[2] -> operand 2 symbol
; */
;/* Missing symbols required by overlay modules */
;extern int err_cnt;
;extern int loc_ctr;
;extern int parse_line();
;extern int emit_code();
;extern int list_line();
;/*  Some trivial functions:  */
;#define	backchr	linptr--		/*  Push back a character.	*/
;#include "R99Ext.h"
;/*
; TMS9900/99105 Cross-Assembler external declarations.
; Refactored for the Small C 2.2 declaration model.  Small C does not
; implement ANSI prototypes, so these declarations intentionally carry
; no parameter lists or return-type promises.
; */
;extern setfiles();
;extern getopr();
;extern getopc();
;extern getopcod();
;extern getattr();
;extern entnam();
;extern putlin();
;extern ishex();
;extern _aton();
;extern putbits();
;extern getchr();
;extern putchr();
;extern putbyt();
;extern putrchr();
;extern rputchr();
;extern slookup();
;extern addsym();
;extern sortsym();
;extern _psop();
;extern _normop();
;extern getname();
;extern symcmp();
;extern asmline();
;extern getitem();
;extern getlin();
;extern source_rewind();
;extern include_source();
;extern push_source();
;extern close_includes();
;extern wipeout();
;extern puthex2();
;extern puthex4();
;extern putdec();
;extern getprec();
;extern markerr();
;extern hash();
;extern toupper();
;extern putabs2();
;extern putabs();
;extern eval();
;extern chkoprat();
;extern flush();
;extern gflush();
;extern rflush();
;extern getnum();
;extern relsyms();
;extern putrel();
;extern putfld();
;extern putsym();
;extern xref_add();
;extern xref_reset();
;extern xref_emit();
;extern lineout();
;extern relout();
;extern relhead();
;extern backitem();
;extern bbsearch();
;extern numopcs();
;extern numoprs();
;extern movchr();
;extern seek();
;extern flushlin();
;extern puts2();
;extern puts3();
;extern putls();
;extern cant();
;extern error2();
;extern error();
;#include "fcntl.h"
;#define _O_RDONLY 0
;#define _O_WRONLY 1
;#define _O_RDWR   2
;#define _O_APPEND 0x0008
;#define _O_CREAT  0x0100
;#define _O_TRUNC  0x0200
;#define _O_EXCL   0x0400
;#define _O_TEXT   0x4000
;#define _O_BINARY 0x8000
;extern char srcname[24];
;extern avail();
;/*  The assembler starts here.  */
;/* Diagnostic only: dump the raw Pass-1 symbol table without altering it. */
;p1dump()
	EVEN
p1dump:
;{
;modstk(newsp,save)
;	int i, j;
;	struct symbtbl *sp;
;	putls("\nPASS 1 SYMBOL TABLE\n");
;modstk(newsp,save)
	AI SP,-6
;constant(lval)
	LI R4,cc1+0
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;	putls("SLOT SYMBOL           VALUE FLAGS\n");
;constant(lval)
	LI R4,cc1+22
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;	for (i = 0; i < SYMBOLS; i++) {
;getloc(sym, off)
	LI R4,4
	A SP,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	CLR R4
;putstk - int
	MOV R4,*R3
cc4:
;getloc(sym, off)
	MOV @4(SP),R4 ;optimised load
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,101
;_cclt
	C R3,R4 ;optimised
	JLT $+6
	B @cc3
;jump(label)
	B @cc5
cc2:
;getloc(sym, off)
	LI R4,4
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;indirect ccgint
	MOV *R4,R4
;inc()
	INC R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;	dec()
	DEC R4
;jump(label)
	B @cc4
cc5:
;		sp = symtbl + i;
;modstk(newsp,save)
;address(ptr)
	LI R4,symtbl
;zpush()
	DECT SP
	MOV R4,*SP
;getloc(sym, off)
	MOV @6(SP),R4 ;optimised load
;scale(type,tag)
	DECT SP
	MOV R3,*SP
;const2(val)
	LI R3,20
	BL @_ccmult##
	MOV *SP+,R3
;zpop()
	MOV *SP+,R3
;	zadd()
	A R3,R4
;puttos()
	MOV R4,*SP
;		if ((sp->symname[0] & 0x7f) != '\0') {
;getloc(sym, off)
	MOV *SP,R4 ;optimised load
;indirect ccgchar
	MOVB *R4,R4
	SRA R4,8
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,127
;zand()
	INV R3
	SZC R3,R4
	MOV R4,R4
	JNE $+6
	B @cc6
;			linptr = linbuf;
;modstk(newsp,save)
;address(ptr)
	LI R4,linbuf
;putmem(sym)
	MOV R4,@linptr
;			linptr = puthex2(i, linptr);
;getloc(sym, off)
	MOV @4(SP),R4 ;optimised load
;zpush()
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @linptr,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puthex2
;modstk(newsp,save)
	AI SP,4
;putmem(sym)
	MOV R4,@linptr
;			*linptr++ = ' ';
;getmem(sym)
	MOV @linptr,R4
;inc()
	INC R4
;putmem(sym)
	MOV R4,@linptr
;	dec()
	DEC R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,32
;putstk - char
	MOVB @2*R4+1(WP),*R3
;			for (j = 0; j < SYMLEN; j++)
;const1(val)
	CLR R4
;put2tos()
	MOV R4,@2(SP)
cc9:
;getloc(sym, off)
	MOV @2(SP),R4 ;optimised load
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,16
;_cclt
	C R3,R4 ;optimised
	JLT $+6
	B @cc8
;jump(label)
	B @cc10
cc7:
;getloc(sym, off)
	LI R4,2
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;indirect ccgint
	MOV *R4,R4
;inc()
	INC R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;	dec()
	DEC R4
;jump(label)
	B @cc9
cc10:
;				*linptr++ = sp->symname[j] & 0x7f;
;getmem(sym)
	MOV @linptr,R4
;inc()
	INC R4
;putmem(sym)
	MOV R4,@linptr
;	dec()
	DEC R4
;zpush()
	DECT SP
	MOV R4,*SP
;getloc(sym, off)
	MOV @2(SP),R4 ;optimised load
;zpush()
	DECT SP
	MOV R4,*SP
;getloc(sym, off)
	MOV @6(SP),R4 ;optimised load
;scale(type,tag)
;zpop()
	MOV *SP+,R3
;	zadd()
	A R3,R4
;indirect ccgchar
	MOVB *R4,R4
	SRA R4,8
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,127
;zand()
	INV R3
	SZC R3,R4
;zpop()
	MOV *SP+,R3
;putstk - char
	MOVB @2*R4+1(WP),*R3
;jump(label)
	B @cc7
cc8:
;			*linptr++ = ' ';
;getmem(sym)
	MOV @linptr,R4
;inc()
	INC R4
;putmem(sym)
	MOV R4,@linptr
;	dec()
	DEC R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,32
;putstk - char
	MOVB @2*R4+1(WP),*R3
;			linptr = puthex4(sp->symvalu, linptr);
;getloc(sym, off)
	MOV *SP,R4 ;optimised load
;const2(val)
	LI R3,16
;	zadd()
	A R3,R4
;indirect ccgint
	MOV *R4,R4
;zpush()
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @linptr,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puthex4
;modstk(newsp,save)
	AI SP,4
;putmem(sym)
	MOV R4,@linptr
;			*linptr++ = ' ';
;getmem(sym)
	MOV @linptr,R4
;inc()
	INC R4
;putmem(sym)
	MOV R4,@linptr
;	dec()
	DEC R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,32
;putstk - char
	MOVB @2*R4+1(WP),*R3
;			linptr = puthex2(sp->symflg, linptr);
;getloc(sym, off)
	MOV *SP,R4 ;optimised load
;const2(val)
	LI R3,18
;	zadd()
	A R3,R4
;indirect ccgchar
	MOVB *R4,R4
	SRA R4,8
;zpush()
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @linptr,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puthex2
;modstk(newsp,save)
	AI SP,4
;putmem(sym)
	MOV R4,@linptr
;			*linptr++ = '\n';
;getmem(sym)
	MOV @linptr,R4
;inc()
	INC R4
;putmem(sym)
	MOV R4,@linptr
;	dec()
	DEC R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,13
;putstk - char
	MOVB @2*R4+1(WP),*R3
;			*linptr = '\0';
;getmem(sym)
	MOV @linptr,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	CLR R4
;putstk - char
	MOVB @2*R4+1(WP),*R3
;			putls(linbuf);
;address(ptr)
	LI R4,linbuf
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;		}
;modstk(newsp,save)
;	}
cc6:
;modstk(newsp,save)
;jump(label)
	B @cc2
cc3:
;}
;modstk(newsp,save)
	AI SP,6
;modstk(newsp,save)
	RET
cc1:	BYTE 13,80,65,83,83,32,49,32,83,89,77,66
	BYTE 79,76,32,84,65,66,76,69,13,0,83,76
	BYTE 79,84,32,83,89,77,66,79,76,32,32,32
	BYTE 32,32,32,32,32,32,32,32,86,65,76,85
	BYTE 69,32,70,76,65,71,83,13,0
	EVEN
;main(argc, argv)
	EVEN
main:
;int argc;
;int *argv;
;{
;	int n, m;
;	unsigned u;
;	putls("\n----------------------------------------------");
;modstk(newsp,save)
	AI SP,-6
;constant(lval)
	LI R4,cc11+0
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;	putls("\nTMS9900 Relocatable Cross-Assembler  vers 2.0\n");
;constant(lval)
	LI R4,cc11+48
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;	putls("Copyright (c) 1980  William C. Colley, III\n");
;constant(lval)
	LI R4,cc11+96
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;	putls("(TMS 99105A version by Alexander. Cameron Jan 1984 and May 2015 )\n");
;constant(lval)
	LI R4,cc11+140
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;	putls("----------------------------------------------\n");
;constant(lval)
	LI R4,cc11+207
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;	putls("Available memory = ");
;constant(lval)
	LI R4,cc11+255
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;	linptr = linbuf;
;address(ptr)
	LI R4,linbuf
;putmem(sym)
	MOV R4,@linptr
;	linptr = putdec(avail(), linptr);
;zcall()
	CALL @avail
;modstk(newsp,save)
;zpush()
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @linptr,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putdec
;modstk(newsp,save)
	AI SP,4
;putmem(sym)
	MOV R4,@linptr
;	*linptr = '\0';
;getmem(sym)
	MOV @linptr,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	CLR R4
;putstk - char
	MOVB @2*R4+1(WP),*R3
;	putls(linbuf);
;address(ptr)
	LI R4,linbuf
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;	putls(" bytes\n");
;constant(lval)
	LI R4,cc11+275
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;	setfiles(argc, argv);
;getloc(sym, off)
	MOV @10(SP),R4 ;optimised load
;zpush()
	DECT SP
	MOV R4,*SP
;getloc(sym, off)
	MOV @10(SP),R4 ;optimised load
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @setfiles
;modstk(newsp,save)
	AI SP,4
;	sympoint = symtbl; /*  Initialize symbol table.	*/
;address(ptr)
	LI R4,symtbl
;putmem(sym)
	MOV R4,@sympoint
;	symend = symtbl + SYMBOLS;
;address(ptr)
	LI R4,symtbl
;const2(val)
	LI R3,2020
;	zadd()
	A R3,R4
;putmem(sym)
	MOV R4,@symend
;	memset(sympoint, '\0', SYMBOLS * sizeof(struct symbtbl)); /* symflg added  */
;getmem(sym)
	MOV @sympoint,R4
;zpush()
	DECT SP
	MOV R4,*SP
;const1(val)
	CLR R4
;zpush()
	DECT SP
	MOV R4,*SP
;const1(val)
	LI R4,2020
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @memset
;modstk(newsp,save)
	AI SP,6
;	memset(itemflg, '\0', 3); /*  Initialize encode buffer	*/
;address(ptr)
	LI R4,itemflg
;zpush()
	DECT SP
	MOV R4,*SP
;const1(val)
	CLR R4
;zpush()
	DECT SP
	MOV R4,*SP
;const1(val)
	LI R4,3
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @memset
;modstk(newsp,save)
	AI SP,6
;	memset(symptr, '\0', 3);
;address(ptr)
	LI R4,symptr
;zpush()
	DECT SP
	MOV R4,*SP
;const1(val)
	CLR R4
;zpush()
	DECT SP
	MOV R4,*SP
;const1(val)
	LI R4,3
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @memset
;modstk(newsp,save)
	AI SP,6
;	ifsp = 0; /*  Initialise if stack.	*/
;const1(val)
	CLR R4
;putmem(sym)
	MOV R4,@ifsp
;	ifstack[ifsp] = 0xffff;
;address(ptr)
	LI R4,ifstack
;zpush()
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @ifsp,R4
;scale(type,tag)
;	doublereg()
	A R4,R4
;zpop()
	MOV *SP+,R3
;	zadd()
	A R3,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,65535
;putstk - int
	MOV R4,*R3
;	hxbytes = 0; /*  Initialise hex generator.	*/
;const1(val)
	CLR R4
;putmem(sym)
	MOVB @2*R4+1(WP),@hxbytes
;	pc = errcount = progsize = 0;
;const1(val)
	CLR R4
;putmem(sym)
	MOV R4,@progsize
;putmem(sym)
	MOV R4,@errcount
;putmem(sym)
	MOV R4,@pc
;	pass = 1;
;const1(val)
	LI R4,1
;putmem(sym)
	MOVB @2*R4+1(WP),@pass
;	if (lstbuf.fd == CONO)
;address(ptr)
	LI R4,lstbuf
;indirect ccgint
	MOV *R4,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,-2
;_cceq
	C R3,R4 ;optimised
	JEQ $+6
	B @cc12
;		puts("Pass 1\n");
;constant(lval)
	LI R4,cc11+283
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;	while (pass != 3) /*  The actual assembly starts here.	*/
cc12:
cc13:
;getmem(sym)
	MOVB @pass,R4
	SRA R4,8 ;ccsxt - sign extend
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,3
;_ccne
	C R3,R4 ;optimised
	JNE $+6
	B @cc14
;	{
;		errcode = ' ';
;modstk(newsp,save)
;const1(val)
	LI R4,32
;putmem(sym)
	MOV R4,@errcode
;		if (!getlin()) {
;zcall()
	CALL @getlin
;modstk(newsp,save)
	BL @_cclneg##
	MOV R4,R4
	JNE $+6
	B @cc15
;			strcpy(linbuf, "\tEND\t\t;You forgot this!\n");
;modstk(newsp,save)
;address(ptr)
	LI R4,linbuf
;zpush()
	DECT SP
	MOV R4,*SP
;constant(lval)
	LI R4,cc11+291
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @strcpy
;modstk(newsp,save)
	AI SP,4
;			linptr = linbuf;
;address(ptr)
	LI R4,linbuf
;putmem(sym)
	MOV R4,@linptr
;			markerr('*');
;const1(val)
	LI R4,42
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @markerr
;modstk(newsp,save)
	INCT SP
;			ifstack[ifsp] = 0xffff;
;address(ptr)
	LI R4,ifstack
;zpush()
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @ifsp,R4
;scale(type,tag)
;	doublereg()
	A R4,R4
;zpop()
	MOV *SP+,R3
;	zadd()
	A R3,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,65535
;putstk - int
	MOV R4,*R3
;		}
;modstk(newsp,save)
;		asmline(); /*  Get binary from line.	*/
cc15:
;zcall()
	CALL @asmline
;modstk(newsp,save)
;		if (pass > 1) {
;getmem(sym)
	MOVB @pass,R4
	SRA R4,8 ;ccsxt - sign extend
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,1
;_ccgt
	C R3,R4 ;optimised
	JGT $+6
	B @cc16
;			lineout(); /*  In pass 2, list line.	*/
;modstk(newsp,save)
;zcall()
	CALL @lineout
;modstk(newsp,save)
;			relout(); /*  In pass 2, build rel file.	*/
;zcall()
	CALL @relout
;modstk(newsp,save)
;		} else
;modstk(newsp,save)
;jump(label)
	B @cc17
cc16:
;			progsize += nbytes; /* keep track of module size */
;getmem(sym)
	MOV @progsize,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	MOV @nbytes,R4
;	zadd()
	A R3,R4
;putmem(sym)
	MOV R4,@progsize
cc17:
;		pc += nbytes;
;getmem(sym)
	MOV @pc,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	MOV @nbytes,R4
;	zadd()
	A R3,R4
;putmem(sym)
	MOV R4,@pc
;		if (pass == 0) /*  This indicates end of pass 1.	*/
;getmem(sym)
	MOVB @pass,R4
	SRA R4,8 ;ccsxt - sign extend
;eq0(label)
	MOV R4,R4
	JEQ $+6
	B @cc18
;		{
;			if (lstbuf.fd == CONO)
;modstk(newsp,save)
;address(ptr)
	LI R4,lstbuf
;indirect ccgint
	MOV *R4,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,-2
;_cceq
	C R3,R4 ;optimised
	JEQ $+6
	B @cc19
;				p1dump(); /*  diagnostic: raw slot view of the table  */
;zcall()
	CALL @p1dump
;modstk(newsp,save)
;			pass = 2;
cc19:
;const1(val)
	LI R4,2
;putmem(sym)
	MOVB @2*R4+1(WP),@pass
;			pc = 0;
;const1(val)
	CLR R4
;putmem(sym)
	MOV R4,@pc
;			errcount = 0; /*  pass 2 re-reports every error  */
;const1(val)
	CLR R4
;putmem(sym)
	MOV R4,@errcount
;			ifsp = 0;
;const1(val)
	CLR R4
;putmem(sym)
	MOV R4,@ifsp
;			ifstack[ifsp] = 0xffff;
;address(ptr)
	LI R4,ifstack
;zpush()
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @ifsp,R4
;scale(type,tag)
;	doublereg()
	A R4,R4
;zpop()
	MOV *SP+,R3
;	zadd()
	A R3,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,65535
;putstk - int
	MOV R4,*R3
;			source_rewind();
;zcall()
	CALL @source_rewind
;modstk(newsp,save)
;			relhead(); /*  module header - must follow the rewind  */
;zcall()
	CALL @relhead
;modstk(newsp,save)
;			if (lstbuf.fd == CONO)
;address(ptr)
	LI R4,lstbuf
;indirect ccgint
	MOV *R4,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,-2
;_cceq
	C R3,R4 ;optimised
	JEQ $+6
	B @cc20
;				puts("Pass 2\n");
;constant(lval)
	LI R4,cc11+316
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;		}
cc20:
;modstk(newsp,save)
;	}
cc18:
;modstk(newsp,save)
;jump(label)
	B @cc13
cc14:
;	/*  print statistics  */
;	putls("Programme size = ");
;constant(lval)
	LI R4,cc11+324
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;linptr = linbuf;
;address(ptr)
	LI R4,linbuf
;putmem(sym)
	MOV R4,@linptr
;linptr = putdec(progsize, linptr);
;getmem(sym)
	MOV @progsize,R4
;zpush()
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @linptr,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putdec
;modstk(newsp,save)
	AI SP,4
;putmem(sym)
	MOV R4,@linptr
;*linptr = '\0';
;getmem(sym)
	MOV @linptr,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	CLR R4
;putstk - char
	MOVB @2*R4+1(WP),*R3
;putls(linbuf);
;address(ptr)
	LI R4,linbuf
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;putls("  ");
;constant(lval)
	LI R4,cc11+342
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;linptr = linbuf;
;address(ptr)
	LI R4,linbuf
;putmem(sym)
	MOV R4,@linptr
;linptr = puthex4(progsize, linptr);
;getmem(sym)
	MOV @progsize,R4
;zpush()
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @linptr,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puthex4
;modstk(newsp,save)
	AI SP,4
;putmem(sym)
	MOV R4,@linptr
;*linptr = '\0';
;getmem(sym)
	MOV @linptr,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	CLR R4
;putstk - char
	MOVB @2*R4+1(WP),*R3
;putls(linbuf);
;address(ptr)
	LI R4,linbuf
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;putls("(Hex)\n");
;constant(lval)
	LI R4,cc11+345
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;	u = (SYMBOLS - nssymbols);
;getmem(sym)
	MOV @nssymbols,R4
;const2(val)
	LI R3,101
;zsub()
	S R4,R3
	MOV R3,R4
;puttos()
	MOV R4,*SP
;	u = 100 * u / SYMBOLS;
;getloc(sym, off)
	MOV *SP,R4 ;optimised load
;const2(val)
	LI R3,100
	BL @_ccmult##
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,101
	BL @_ccdiv##
;puttos()
	MOV R4,*SP
;	putls("\nSymbol table use factor = ");
;constant(lval)
	LI R4,cc11+352
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;linptr = linbuf;
;address(ptr)
	LI R4,linbuf
;putmem(sym)
	MOV R4,@linptr
;linptr = putdec(100 - u, linptr);
;getloc(sym, off)
	MOV *SP,R4 ;optimised load
;const2(val)
	LI R3,100
;zsub()
	S R4,R3
	MOV R3,R4
;zpush()
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @linptr,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putdec
;modstk(newsp,save)
	AI SP,4
;putmem(sym)
	MOV R4,@linptr
;*linptr = '\0';
;getmem(sym)
	MOV @linptr,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	CLR R4
;putstk - char
	MOVB @2*R4+1(WP),*R3
;putls(linbuf);
;address(ptr)
	LI R4,linbuf
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;putls("%\n");
;constant(lval)
	LI R4,cc11+380
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putls
;modstk(newsp,save)
	INCT SP
;	/*  List number of errors.	*/
;	linptr = linbuf;
;address(ptr)
	LI R4,linbuf
;putmem(sym)
	MOV R4,@linptr
;	*linptr++ = '\n';
;getmem(sym)
	MOV @linptr,R4
;inc()
	INC R4
;putmem(sym)
	MOV R4,@linptr
;	dec()
	DEC R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,13
;putstk - char
	MOVB @2*R4+1(WP),*R3
;	if (errcount == 0)
;getmem(sym)
	MOV @errcount,R4
;eq0(label)
	MOV R4,R4
	JEQ $+6
	B @cc21
;		strcpy(linptr, "No");
;getmem(sym)
	MOV @linptr,R4
;zpush()
	DECT SP
	MOV R4,*SP
;constant(lval)
	LI R4,cc11+383
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @strcpy
;modstk(newsp,save)
	AI SP,4
;	else {
;jump(label)
	B @cc22
cc21:
;		linptr = putdec(errcount, linptr);
;modstk(newsp,save)
;getmem(sym)
	MOV @errcount,R4
;zpush()
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @linptr,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putdec
;modstk(newsp,save)
	AI SP,4
;putmem(sym)
	MOV R4,@linptr
;		*linptr = '\0';
;getmem(sym)
	MOV @linptr,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	CLR R4
;putstk - char
	MOVB @2*R4+1(WP),*R3
;	}
;modstk(newsp,save)
cc22:
;	strcat(linbuf, " error(s).\n");
;address(ptr)
	LI R4,linbuf
;zpush()
	DECT SP
	MOV R4,*SP
;constant(lval)
	LI R4,cc11+386
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @strcat
;modstk(newsp,save)
	AI SP,4
;	puts(linbuf);
;address(ptr)
	LI R4,linbuf
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puts
;modstk(newsp,save)
	INCT SP
;	if (lstbuf.fd != CONO && lstbuf.fd != NOFILE) {
;address(ptr)
	LI R4,lstbuf
;indirect ccgint
	MOV *R4,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,-2
;_ccne
	C R3,R4 ;optimised
	JNE $+6
	B @cc24
;address(ptr)
	LI R4,lstbuf
;indirect ccgint
	MOV *R4,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	SETO R4
;_ccne
	C R3,R4 ;optimised
	JNE $+6
	B @cc24
;const1(val)
	LI R4,1
;jump(label)
	B @cc25
cc24:
;const1(val)
	CLR R4
cc25:
	MOV R4,R4
	JNE $+6
	B @cc23
;		putlin(linbuf, &lstbuf);
;modstk(newsp,save)
;address(ptr)
	LI R4,linbuf
;zpush()
	DECT SP
	MOV R4,*SP
;address(ptr)
	LI R4,lstbuf
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putlin
;modstk(newsp,save)
	AI SP,4
;		/*		putchr('\f',&lstbuf); */
;	}
;modstk(newsp,save)
;	if (lstbuf.fd != NOFILE) /*  If needed, sort and list
;	 symbol table.		*/
cc23:
;address(ptr)
	LI R4,lstbuf
;indirect ccgint
	MOV *R4,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	SETO R4
;_ccne
	C R3,R4 ;optimised
	JNE $+6
	B @cc26
;	{
;		n = nssymbols;
;modstk(newsp,save)
;getloc(sym, off)
	LI R4,4
	A SP,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	MOV @nssymbols,R4
;putstk - int
	MOV R4,*R3
;		sympoint = symtbl;
;address(ptr)
	LI R4,symtbl
;putmem(sym)
	MOV R4,@sympoint
;		while (n > 0) {
cc27:
;getloc(sym, off)
	MOV @4(SP),R4 ;optimised load
;gt0(label)
	MOV R4,R4
	JGT $+6
	B @cc28
;			linptr = linbuf;
;modstk(newsp,save)
;address(ptr)
	LI R4,linbuf
;putmem(sym)
	MOV R4,@linptr
;			for (m = 0; m < 4; m++) {
;const1(val)
	CLR R4
;put2tos()
	MOV R4,@2(SP)
cc31:
;getloc(sym, off)
	MOV @2(SP),R4 ;optimised load
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,4
;_cclt
	C R3,R4 ;optimised
	JLT $+6
	B @cc30
;jump(label)
	B @cc32
cc29:
;getloc(sym, off)
	LI R4,2
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;indirect ccgint
	MOV *R4,R4
;inc()
	INC R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;	dec()
	DEC R4
;jump(label)
	B @cc31
cc32:
;				memcpy(linptr, sympoint->symname, SYMLEN);
;modstk(newsp,save)
;getmem(sym)
	MOV @linptr,R4
;zpush()
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @sympoint,R4
;zpush()
	DECT SP
	MOV R4,*SP
;const1(val)
	LI R4,16
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @memcpy
;modstk(newsp,save)
	AI SP,6
;				linptr += SYMLEN;					/* Don't know why this works */
;getmem(sym)
	MOV @linptr,R4
;const2(val)
	LI R3,16
;	zadd()
	A R3,R4
;putmem(sym)
	MOV R4,@linptr
;				*linptr++ = ' ';
;getmem(sym)
	MOV @linptr,R4
;inc()
	INC R4
;putmem(sym)
	MOV R4,@linptr
;	dec()
	DEC R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,32
;putstk - char
	MOVB @2*R4+1(WP),*R3
;				*linptr++ = ' ';
;getmem(sym)
	MOV @linptr,R4
;inc()
	INC R4
;putmem(sym)
	MOV R4,@linptr
;	dec()
	DEC R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,32
;putstk - char
	MOVB @2*R4+1(WP),*R3
;				linptr = puthex4(sympoint->symvalu, linptr);
;getmem(sym)
	MOV @sympoint,R4
;const2(val)
	LI R3,16
;	zadd()
	A R3,R4
;indirect ccgint
	MOV *R4,R4
;zpush()
	DECT SP
	MOV R4,*SP
;getmem(sym)
	MOV @linptr,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @puthex4
;modstk(newsp,save)
	AI SP,4
;putmem(sym)
	MOV R4,@linptr
;				if (sympoint->symflg & EXTBIT)
;getmem(sym)
	MOV @sympoint,R4
;const2(val)
	LI R3,18
;	zadd()
	A R3,R4
;indirect ccgchar
	MOVB *R4,R4
	SRA R4,8
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,8
;zand()
	INV R3
	SZC R3,R4
	MOV R4,R4
	JNE $+6
	B @cc33
;					*linptr++ = '*'; /* mark external */
;getmem(sym)
	MOV @linptr,R4
;inc()
	INC R4
;putmem(sym)
	MOV R4,@linptr
;	dec()
	DEC R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,42
;putstk - char
	MOVB @2*R4+1(WP),*R3
;				else if (sympoint->symflg & RELBIT)
;jump(label)
	B @cc34
cc33:
;getmem(sym)
	MOV @sympoint,R4
;const2(val)
	LI R3,18
;	zadd()
	A R3,R4
;indirect ccgchar
	MOVB *R4,R4
	SRA R4,8
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,1
;zand()
	INV R3
	SZC R3,R4
	MOV R4,R4
	JNE $+6
	B @cc35
;					*linptr++ = '\''; /* mark relocatable */
;getmem(sym)
	MOV @linptr,R4
;inc()
	INC R4
;putmem(sym)
	MOV R4,@linptr
;	dec()
	DEC R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,39
;putstk - char
	MOVB @2*R4+1(WP),*R3
;				else
;jump(label)
	B @cc36
cc35:
;					*linptr++ = ' ';
;getmem(sym)
	MOV @linptr,R4
;inc()
	INC R4
;putmem(sym)
	MOV R4,@linptr
;	dec()
	DEC R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,32
;putstk - char
	MOVB @2*R4+1(WP),*R3
cc36:
cc34:
;				*linptr++ = ' ';
;getmem(sym)
	MOV @linptr,R4
;inc()
	INC R4
;putmem(sym)
	MOV R4,@linptr
;	dec()
	DEC R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,32
;putstk - char
	MOVB @2*R4+1(WP),*R3
;				*linptr++ = ' ';
;getmem(sym)
	MOV @linptr,R4
;inc()
	INC R4
;putmem(sym)
	MOV R4,@linptr
;	dec()
	DEC R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,32
;putstk - char
	MOVB @2*R4+1(WP),*R3
;				sympoint++;
;getmem(sym)
	MOV @sympoint,R4
;const2(val)
	LI R3,20
;	zadd()
	A R3,R4
;putmem(sym)
	MOV R4,@sympoint
;const2(val)
	LI R3,-20
;	zadd()
	A R3,R4
;				if (--n <= 0)
;getloc(sym, off)
	LI R4,4
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;indirect ccgint
	MOV *R4,R4
;	dec()
	DEC R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;le0(label)
	CI R4,0
	JLE $+6
	B @cc37
;					break;
;modstk(newsp,save)
;jump(label)
	B @cc30
;			}
cc37:
;modstk(newsp,save)
;jump(label)
	B @cc29
cc30:
;			*linptr++ = '\n';
;getmem(sym)
	MOV @linptr,R4
;inc()
	INC R4
;putmem(sym)
	MOV R4,@linptr
;	dec()
	DEC R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,13
;putstk - char
	MOVB @2*R4+1(WP),*R3
;			*linptr = '\0';
;getmem(sym)
	MOV @linptr,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	CLR R4
;putstk - char
	MOVB @2*R4+1(WP),*R3
;			putlin(linbuf, &lstbuf);
;address(ptr)
	LI R4,linbuf
;zpush()
	DECT SP
	MOV R4,*SP
;address(ptr)
	LI R4,lstbuf
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putlin
;modstk(newsp,save)
	AI SP,4
;		}
;modstk(newsp,save)
;jump(label)
	B @cc27
cc28:
;		putchr(CPMEOF, &lstbuf);
;const1(val)
	LI R4,26
;zpush()
	DECT SP
	MOV R4,*SP
;address(ptr)
	LI R4,lstbuf
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @putchr
;modstk(newsp,save)
	AI SP,4
;	}
;modstk(newsp,save)
;	flush(&lstbuf);
cc26:
;address(ptr)
	LI R4,lstbuf
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @flush
;modstk(newsp,save)
	INCT SP
;	fclose(sorbuf.fd);
;address(ptr)
	LI R4,sorbuf
;indirect ccgint
	MOV *R4,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @fclose
;modstk(newsp,save)
	INCT SP
;	//wipeout("\r");
;}
;modstk(newsp,save)
	AI SP,6
;modstk(newsp,save)
	RET
cc11:	BYTE 13,45,45,45,45,45,45,45,45,45,45,45
	BYTE 45,45,45,45,45,45,45,45,45,45,45,45
	BYTE 45,45,45,45,45,45,45,45,45,45,45,45
	BYTE 45,45,45,45,45,45,45,45,45,45,45,0
	BYTE 13,84,77,83,57,57,48,48,32,82,101,108
	BYTE 111,99,97,116,97,98,108,101,32,67,114,111
	BYTE 115,115,45,65,115,115,101,109,98,108,101,114
	BYTE 32,32,118,101,114,115,32,50,46,48,13,0
	BYTE 67,111,112,121,114,105,103,104,116,32,40,99
	BYTE 41,32,49,57,56,48,32,32,87,105,108,108
	BYTE 105,97,109,32,67,46,32,67,111,108,108,101
	BYTE 121,44,32,73,73,73,13,0,40,84,77,83
	BYTE 32,57,57,49,48,53,65,32,118,101,114,115
	BYTE 105,111,110,32,98,121,32,65,108,101,120,97
	BYTE 110,100,101,114,46,32,67,97,109,101,114,111
	BYTE 110,32,74,97,110,32,49,57,56,52,32,97
	BYTE 110,100,32,77,97,121,32,50,48,49,53,32
	BYTE 41,13,0,45,45,45,45,45,45,45,45,45
	BYTE 45,45,45,45,45,45,45,45,45,45,45,45
	BYTE 45,45,45,45,45,45,45,45,45,45,45,45
	BYTE 45,45,45,45,45,45,45,45,45,45,45,45
	BYTE 45,13,0,65,118,97,105,108,97,98,108,101
	BYTE 32,109,101,109,111,114,121,32,61,32,0,32
	BYTE 98,121,116,101,115,13,0,80,97,115,115,32
	BYTE 49,13,0,9,69,78,68,9,9,59,89,111
	BYTE 117,32,102,111,114,103,111,116,32,116,104,105
	BYTE 115,33,13,0,80,97,115,115,32,50,13,0
	BYTE 80,114,111,103,114,97,109,109,101,32,115,105
	BYTE 122,101,32,61,32,0,32,32,0,40,72,101
	BYTE 120,41,13,0,13,83,121,109,98,111,108,32
	BYTE 116,97,98,108,101,32,117,115,101,32,102,97
	BYTE 99,116,111,114,32,61,32,0,37,13,0,78
	BYTE 111,0,32,101,114,114,111,114,40,115,41,46
	BYTE 13,0
;/*
; Function to set up the file structure.  Routine is called with
; the original argc and argv from main().
; */
;setfiles(argc, argv)
	EVEN
setfiles:
;int argc;
;int *argv;
;{
;	char sorfname[24], lstfname[24], hexfname[24], *tptr, *arg;
;	int noobj;
;	/*
;	 *  R99 <file> [switches]
;	 *
;	 *      -L      listing to <file>.L99      (also -LA..-LD, -L-)
;	 *      -LX     listing to the console
;	 *      -LY     listing to the list device
;	 *      -N      no object file - syntax check only
;	 *      -R, -H  object to <file>.R99: this is the DEFAULT, and the
;	 *              switch is still accepted so old command lines work
;	 *      -S      accepted and ignored; the source name is the file name
;	 *
;	 *  The .R99 object is produced by DEFAULT.  A leading '-' is optional
;	 *  on every switch, so the older bare forms (LX, L-, H-) still work.
;	 */
;	if (--argc == 0)
;modstk(newsp,save)
	AI SP,-78
;getloc(sym, off)
	LI R4,82
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;indirect ccgint
	MOV *R4,R4
;	dec()
	DEC R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;eq0(label)
	MOV R4,R4
	JEQ $+6
	B @cc39
;		wipeout("\nNo file info supplied.\n");
;constant(lval)
	LI R4,cc38+0
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @wipeout
;modstk(newsp,save)
	INCT SP
;	argv++;
cc39:
;getloc(sym, off)
	LI R4,80
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;indirect ccgint
	MOV *R4,R4
;inct()
	INCT R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;	dect()
	DECT R4
;	sorbuf.fd = lstbuf.fd = hexbuf.fd = NOFILE;
;address(ptr)
	LI R4,sorbuf
;zpush()
	DECT SP
	MOV R4,*SP
;address(ptr)
	LI R4,lstbuf
;zpush()
	DECT SP
	MOV R4,*SP
;address(ptr)
	LI R4,hexbuf
;zpush()
	MOV R4,R3 ;optimised push/pop
	SETO R4
;putstk - int
	MOV R4,*R3
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;	lstbuf.pointr = lstbuf.space;
;address(ptr)
	LI R4,lstbuf
;inct()
	INCT R4
;zpush()
	DECT SP
	MOV R4,*SP
;address(ptr)
	LI R4,lstbuf
;const2(val)
	LI R3,6
;	zadd()
	A R3,R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;	hexbuf.pointr = hexbuf.space;
;address(ptr)
	LI R4,hexbuf
;inct()
	INCT R4
;zpush()
	DECT SP
	MOV R4,*SP
;address(ptr)
	LI R4,hexbuf
;const2(val)
	LI R3,6
;	zadd()
	A R3,R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;	noobj = 0;
;const1(val)
	CLR R4
;puttos()
	MOV R4,*SP
;	sorfname[0] = '\0';
;getloc(sym, off)
;Char offset
	LI R4,55
	A SP,R4
;zpush()
	MOV R4,R3 ;optimised push/pop
	CLR R4
;putstk - char
	MOVB @2*R4+1(WP),*R3
;	arg = *argv++;
;getloc(sym, off)
	LI R4,80
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;indirect ccgint
	MOV *R4,R4
;inct()
	INCT R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;	dect()
	DECT R4
;indirect ccgint
	MOV *R4,R4
;put2tos()
	MOV R4,@2(SP)
;	--argc;
;getloc(sym, off)
	LI R4,82
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;indirect ccgint
	MOV *R4,R4
;	dec()
	DEC R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;	strcpy(progname, arg); /* copy programme name */
;address(ptr)
	LI R4,progname
;zpush()
	DECT SP
	MOV R4,*SP
;getloc(sym, off)
	MOV @4(SP),R4 ;optimised load
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @strcpy
;modstk(newsp,save)
	AI SP,4
;	strcat(sorfname, arg);
;getloc(sym, off)
;Char offset
	LI R4,55
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;getloc(sym, off)
	MOV @4(SP),R4 ;optimised load
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @strcat
;modstk(newsp,save)
	AI SP,4
;	for (tptr = sorfname; *tptr != '\0'; tptr++)
;getloc(sym, off)
	LI R4,4
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;getloc(sym, off)
;Char offset
	LI R4,57
	A SP,R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
cc42:
;getloc(sym, off)
	MOV @4(SP),R4 ;optimised load
;indirect ccgchar
	MOVB *R4,R4
	SRA R4,8
	MOV R4,R4
	JNE $+6
	B @cc41
;jump(label)
	B @cc43
cc40:
;getloc(sym, off)
	LI R4,4
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;indirect ccgint
	MOV *R4,R4
;inc()
	INC R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;	dec()
	DEC R4
;jump(label)
	B @cc42
cc43:
;		if (*tptr == '.')
;getloc(sym, off)
	MOV @4(SP),R4 ;optimised load
;indirect ccgchar
	MOVB *R4,R4
	SRA R4,8
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,46
;_cceq
	C R3,R4 ;optimised
	JEQ $+6
	B @cc44
;			*tptr = '\0';
;getloc(sym, off)
	MOV @4(SP),R4 ;optimised load
;zpush()
	MOV R4,R3 ;optimised push/pop
	CLR R4
;putstk - char
	MOVB @2*R4+1(WP),*R3
;	strcpy(lstfname, sorfname);
cc44:
;jump(label)
	B @cc40
cc41:
;getloc(sym, off)
;Char offset
	LI R4,31
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;getloc(sym, off)
;Char offset
	LI R4,57
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @strcpy
;modstk(newsp,save)
	AI SP,4
;	strcpy(hexfname, lstfname);
;getloc(sym, off)
;Char offset
	LI R4,7
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;getloc(sym, off)
;Char offset
	LI R4,33
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @strcpy
;modstk(newsp,save)
	AI SP,4
;	strcat(sorfname, ".A99");
;getloc(sym, off)
;Char offset
	LI R4,55
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;constant(lval)
	LI R4,cc38+25
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @strcat
;modstk(newsp,save)
	AI SP,4
;	strcat(lstfname, ".L99");
;getloc(sym, off)
;Char offset
	LI R4,31
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;constant(lval)
	LI R4,cc38+30
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @strcat
;modstk(newsp,save)
	AI SP,4
;	strcat(hexfname, ".R99");
;getloc(sym, off)
;Char offset
	LI R4,7
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;constant(lval)
	LI R4,cc38+35
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @strcat
;modstk(newsp,save)
	AI SP,4
;	while (argc > 0) {
cc45:
;getloc(sym, off)
	MOV @82(SP),R4 ;optimised load
;gt0(label)
	MOV R4,R4
	JGT $+6
	B @cc46
;		arg = *argv++;
;modstk(newsp,save)
;getloc(sym, off)
	LI R4,80
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;indirect ccgint
	MOV *R4,R4
;inct()
	INCT R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;	dect()
	DECT R4
;indirect ccgint
	MOV *R4,R4
;put2tos()
	MOV R4,@2(SP)
;		--argc;
;getloc(sym, off)
	LI R4,82
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;indirect ccgint
	MOV *R4,R4
;	dec()
	DEC R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;		if (*arg == '-')
;getloc(sym, off)
	MOV @2(SP),R4 ;optimised load
;indirect ccgchar
	MOVB *R4,R4
	SRA R4,8
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,45
;_cceq
	C R3,R4 ;optimised
	JEQ $+6
	B @cc47
;			arg++;
;getloc(sym, off)
	LI R4,2
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;indirect ccgint
	MOV *R4,R4
;inc()
	INC R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;	dec()
	DEC R4
;		switch (*arg++) {
cc47:
;getloc(sym, off)
	LI R4,2
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;indirect ccgint
	MOV *R4,R4
;inc()
	INC R4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;	dec()
	DEC R4
;indirect ccgchar
	MOVB *R4,R4
	SRA R4,8
;jump(label)
	B @cc50
;		case 'L':
;modstk(newsp,save)
cc51:
;			switch (*arg) {
;getloc(sym, off)
	MOV @2(SP),R4 ;optimised load
;indirect ccgchar
	MOVB *R4,R4
	SRA R4,8
;jump(label)
	B @cc54
;			case 'X':
;modstk(newsp,save)
cc55:
;				lstbuf.fd = CONO;
;address(ptr)
	LI R4,lstbuf
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,-2
;putstk - int
	MOV R4,*R3
;				break;
;modstk(newsp,save)
;jump(label)
	B @cc53
;			case 'Y':
cc56:
;				lstbuf.fd = LST;
;address(ptr)
	LI R4,lstbuf
;zpush()
	MOV R4,R3 ;optimised push/pop
	LI R4,-3
;putstk - int
	MOV R4,*R3
;				break;
;modstk(newsp,save)
;jump(label)
	B @cc53
;			default:
cc57:
;				if ((lstbuf.fd = fopen(lstfname, "w")) == 0)
;address(ptr)
	LI R4,lstbuf
;zpush()
	DECT SP
	MOV R4,*SP
;getloc(sym, off)
;Char offset
	LI R4,33
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;constant(lval)
	LI R4,cc38+40
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @fopen
;modstk(newsp,save)
	AI SP,4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;eq0(label)
	MOV R4,R4
	JEQ $+6
	B @cc58
;					wipeout("\nCan't open list.\n");
;constant(lval)
	LI R4,cc38+42
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @wipeout
;modstk(newsp,save)
	INCT SP
;				break;
cc58:
;modstk(newsp,save)
;jump(label)
	B @cc53
;			}
;modstk(newsp,save)
;jump(label)
	B @cc53
cc54:
	CALL @_ccswitc##
	WORD cc55,88
	WORD cc56,89
	WORD 0
;jump(label)
	B @cc57
cc53:
;			break;
;modstk(newsp,save)
;jump(label)
	B @cc49
;		case 'N':
cc59:
;			noobj = 1;
;const1(val)
	LI R4,1
;puttos()
	MOV R4,*SP
;			break;
;modstk(newsp,save)
;jump(label)
	B @cc49
;		case 'H':
cc60:
;		case 'R':
cc61:
;			noobj = 0; /* the object is the default anyway */
;const1(val)
	CLR R4
;puttos()
	MOV R4,*SP
;			break;
;modstk(newsp,save)
;jump(label)
	B @cc49
;		case 'S':
cc62:
;			break; /* the source name is the file argument */
;modstk(newsp,save)
;jump(label)
	B @cc49
;		default:
cc63:
;			wipeout("\nIllegal command line.\n");
;constant(lval)
	LI R4,cc38+61
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @wipeout
;modstk(newsp,save)
	INCT SP
;		}
;modstk(newsp,save)
;jump(label)
	B @cc49
cc50:
	CALL @_ccswitc##
	WORD cc51,76
	WORD cc59,78
	WORD cc60,72
	WORD cc61,82
	WORD cc62,83
	WORD 0
;jump(label)
	B @cc63
cc49:
;	}
;modstk(newsp,save)
;jump(label)
	B @cc45
cc46:
;	strcpy(srcname, sorfname);
;address(ptr)
	LI R4,srcname
;zpush()
	DECT SP
	MOV R4,*SP
;getloc(sym, off)
;Char offset
	LI R4,57
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @strcpy
;modstk(newsp,save)
	AI SP,4
;	if ((sorbuf.fd = fopen(sorfname, "r")) == 0)
;address(ptr)
	LI R4,sorbuf
;zpush()
	DECT SP
	MOV R4,*SP
;getloc(sym, off)
;Char offset
	LI R4,57
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;constant(lval)
	LI R4,cc38+85
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @fopen
;modstk(newsp,save)
	AI SP,4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;eq0(label)
	MOV R4,R4
	JEQ $+6
	B @cc64
;		wipeout("\nCan't open source.\n");
;constant(lval)
	LI R4,cc38+87
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @wipeout
;modstk(newsp,save)
	INCT SP
;	source_rewind();
cc64:
;zcall()
	CALL @source_rewind
;modstk(newsp,save)
;	/*  Open the object LAST, so a source that will not open cannot
;	    leave an empty .R99 behind.  */
;	if (noobj == 0)
;getloc(sym, off)
	MOV *SP,R4 ;optimised load
;eq0(label)
	MOV R4,R4
	JEQ $+6
	B @cc65
;		if ((hexbuf.fd = fopen(hexfname, "w")) == 0)
;address(ptr)
	LI R4,hexbuf
;zpush()
	DECT SP
	MOV R4,*SP
;getloc(sym, off)
;Char offset
	LI R4,9
	A SP,R4
;zpush()
	DECT SP
	MOV R4,*SP
;constant(lval)
	LI R4,cc38+108
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @fopen
;modstk(newsp,save)
	AI SP,4
;zpop()
	MOV *SP+,R3
;putstk - int
	MOV R4,*R3
;eq0(label)
	MOV R4,R4
	JEQ $+6
	B @cc66
;			wipeout("\nCan't open R99 file.\n");
;constant(lval)
	LI R4,cc38+110
;zpush()
	DECT SP
	MOV R4,*SP
;zcall()
	CALL @wipeout
;modstk(newsp,save)
	INCT SP
;}
cc66:
cc65:
;modstk(newsp,save)
	AI SP,78
;modstk(newsp,save)
	RET
cc38:	BYTE 13,78,111,32,102,105,108,101,32,105,110,102
	BYTE 111,32,115,117,112,112,108,105,101,100,46,13
	BYTE 0,46,65,57,57,0,46,76,57,57,0,46
	BYTE 82,57,57,0,119,0,13,67,97,110,39,116
	BYTE 32,111,112,101,110,32,108,105,115,116,46,13
	BYTE 0,13,73,108,108,101,103,97,108,32,99,111
	BYTE 109,109,97,110,100,32,108,105,110,101,46,13
	BYTE 0,114,0,13,67,97,110,39,116,32,111,112
	BYTE 101,110,32,115,111,117,114,99,101,46,13,0
	BYTE 119,0,13,67,97,110,39,116,32,111,112,101
	BYTE 110,32,82,57,57,32,102,105,108,101,46,13
	BYTE 0
	EVEN
	EXT flushlin
	EXT oldvalu
	EXT entnam
	EXT hxlnbuf
	EXT errcount
	EXT hash
	EXT _aton
	EXT getprec
	EXT strcpy
	EXT putsym
	EXT wipeout
	EXT error2
	EXT quitflag
	EXT symend
	EXT bptr
	EXT symbol
	EXT emit_code
	EXT symcmp
	EXT include_source
	EXT ifsp
	EXT eval
	EXT bbsearch
	ENT main
	EXT putrchr
	EXT rputchr
	EXT memcpy
	EXT nssymbols
	EXT hxlnptr
	EXT chkoprat
	EXT item
	EXT symtbl
	EXT linbuf
	EXT evalerr
	EXT pass
	EXT seek
	EXT memset
	EXT source_rewind
	EXT symptr
	EXT err_cnt
	EXT field
	EXT getopcod
	EXT avail
	EXT ifstack
	EXT _psop
	EXT sortsym
	EXT close_includes
	EXT extflg
	EXT linptr
	EXT backitem
	EXT errcode
	EXT relsyms
	EXT addsym
	EXT push_source
	EXT toupper
	EXT puts
	EXT nbytes
	EXT markerr
	EXT relflg
	EXT type
	EXT sympoint
	EXT fopen
	EXT quoteflg
	EXT flush
	EXT loc_ctr
	EXT error
	EXT backflg
	EXT directok
	EXT progname
	EXT _normop
	EXT ishex
	EXT relout
	EXT movchr
	EXT xref_emit
	EXT binbuf
	EXT rflush
	EXT lstbuf
	EXT xref_add
	EXT xref_reset
	EXT lineout
	EXT pc
	EXT itemflg
	ENT p1dump
	EXT fclose
	EXT chksum
	EXT numopcs
	EXT srcname
	EXT progsize
	EXT addrmode
	EXT address
	EXT numoprs
	EXT putabs2
	EXT puts2
	EXT puts3
	EXT parse_line
	EXT getchr
	EXT putabs
	EXT putdec
	EXT slookup
	EXT oldattr
	EXT asmline
	EXT sorbuf
	EXT putchr
	EXT getlin
	EXT gflush
	EXT curdrive
	EXT putfld
	EXT putls
	EXT outchunk
	EXT outrem
	EXT getopc
	EXT getattr
	EXT getname
	ENT setfiles
	EXT puthex2
	EXT puthex4
	EXT relhead
	EXT getopr
	EXT putbyt
	EXT putlin
	EXT getnum
	EXT hexflg
	EXT hexbuf
	EXT putbits
	EXT getitem
	EXT putrel
	EXT cant
	EXT strcat
	EXT hxbytes
	EXT entflg
	EXT list_line
	EVEN
	END

; --- End of Compilation ---
